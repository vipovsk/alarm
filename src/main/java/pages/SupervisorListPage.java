package pages;

import org.openqa.selenium.WebDriver;

public class SupervisorListPage {
    private WebDriver driver;

    public SupervisorListPage(WebDriver driver) {
        this.driver = driver;
    }
}
